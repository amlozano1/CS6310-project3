CREATE TABLE APP.SIMULATION (NAME VARCHAR(50) NOT NULL PRIMARY KEY, GRID_SPACING SMALLINT, TIME_STEP INTEGER, LENGTH SMALLINT, ORBITAL_ECCENTRICITY FLOAT, AXIAL_TILT FLOAT)
CREATE TABLE APP.SIMULATION_RESULT (ID INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY,SIMULATION VARCHAR(50) NOT NULL,GRIDX SMALLINT NOT NULL,GRIDY SMALLINT NOT NULL,TEMPERATURE FLOAT NOT NULL)
ALTER TABLE APP.SIMULATION_RESULT ADD FOREIGN KEY (SIMULATION) REFERENCES APP.SIMULATION(NAME)